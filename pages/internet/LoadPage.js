import { BasePage } from '../base/BasePage.js';

/**
 * Description placeholder
 *
 * @export
 * @class LoadPage
 * @typedef {LoadPage}
 * @extends {BasePage}
 */
export class LoadPage extends BasePage {
  constructor(page) {
    super(page);
  }
}